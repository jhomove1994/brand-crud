export enum SUCCESS {
    RESOURCE_CREATED = '$s created',
    RESOURCE_UPDATED = '$s updated',
    SUCCESS = 'Success',
}

export enum ERROR {
    UNEXPECTED = 'An unexpected error occurred',
    RESOURCE_ALREADY_EXISTS = '$s already exists',
}
